
# 《红楼梦》题库JSON重构方案 - 完整报告

## 📊 项目概览

| 指标 | 数值 |
|------|------|
| 总题目数 | 99 道 |
| 来源文件数 | 5 个TXT文件 |
| 题型种类 | 8 种 |
| 知识分类 | 5 大类 |
| 有答案题目 | 6 道 (6.1%) |
| 复合题数量 | 42 道 |

---

## 🔄 核心改进

### 1. 分类体系升级

#### 旧版本（3类）
- 选择题
- 简答题  
- 分析题

#### 新版本（8类）
| 类型代码 | 中文名称 | 数量 |
|----------|----------|------|
| single_choice | 单选题 | 29 |
| multiple_choice | 多选题 | 0 |
| fill_blank | 填空题 | 0 |
| short_answer | 简答题 | 14 |
| analysis | 分析题 | 5 |
| essay | 作文题 | 6 |
| composite | 复合题 | 42 |
| language_expr | 语言表达 | 3 |

### 2. 新增知识维度分类

| 分类 | 数量 | 占比 |
|------|------|------|
| 人物形象 | 40 | 40.4% |
| 情节考察 | 29 | 29.3% |
| 诗词鉴赏 | 20 | 20.2% |
| 阅读理解 | 9 | 9.1% |
| 跨领域结合 | 1 | 1.0% |

### 3. 数据结构规范化

#### 字段对比

| 功能 | 旧版本 | 新版本 |
|------|--------|--------|
| 题目ID | id | ✅ id（保持兼容） |
| 题型分类 | type（3种） | ✅ type（8种） |
| 知识分类 | ❌ 无 | ✅ category.primary/secondary |
| 难度分级 | ❌ 无 | ✅ difficulty（easy/medium/hard） |
| 题目来源 | ❌ 无 | ✅ source |
| 标签系统 | ❌ 无 | ✅ tags（数组） |
| 题干 | question | ✅ content.stem |
| 选项 | options（字符串数组） | ✅ content.options（对象数组） |
| 答案 | answer（字符串） | ✅ answer.correct |
| 解析 | ❌ 无 | ✅ answer.analysis |
| 评分要点 | ❌ 无 | ✅ answer.keyPoints |
| 复合题支持 | ❌ 无 | ✅ content.subQuestions |
| 阅读材料 | ❌ 无 | ✅ content.material |
| 统计信息 | ❌ 无 | ✅ statistics |
| 元数据 | ❌ 无 | ✅ metadata（完整） |

---

## 📁 输出文件

| 文件名 | 说明 |
|--------|------|
| `hongloumeng_questions_v2.json` | 重构后的完整题库（110KB） |
| `题库重构方案与后续建议.md` | 详细的使用建议和后续处理指南 |
| `题库重构对比分析.png` | 可视化对比图表 |

---

## ⚠️ 已知问题与建议

### 高优先级

1. **答案覆盖率不足（6.1%）**
   - 现状：仅6道题有答案
   - 建议：从原始TXT文件中提取答案并补充

2. **复合题子题类型需确认**
   - 现状：42道复合题已识别，但子题类型需要人工审核
   - 建议：逐题检查并修正子题类型

3. **标签系统未启用**
   - 现状：0/99题有标签
   - 建议：添加人物、情节、考点等标签

### 中优先级

4. **二级分类待细化**
   - 建议：为每个一级分类添加二级分类

5. **难度自动判断需校准**
   - 现状：基于文本长度自动判断
   - 建议：人工审核并调整

---

## 🚀 快速开始

### 1. 加载JSON

```javascript
const fs = require('fs');
const data = JSON.parse(fs.readFileSync('hongloumeng_questions_v2.json', 'utf8'));

console.log(`共加载 ${data.metadata.statistics.totalQuestions} 道题目`);
```

### 2. 按分类筛选

```javascript
// 筛选人物形象类的分析题
const filtered = data.questions.filter(q => 
  q.category.primary === '人物形象' && 
  q.type === 'analysis'
);
```

### 3. 获取复合题

```javascript
const compositeQuestions = data.questions.filter(q => q.type === 'composite');

// 遍历子题
compositeQuestions.forEach(q => {
  console.log(`大题: ${q.content.material ? q.content.material.substring(0, 50) : 'N/A'}...`);
  q.content.subQuestions.forEach(sub => {
    console.log(`  子题${sub.order}: ${sub.stem.substring(0, 50)}...`);
  });
});
```

---

## 🛡️ 数据质量保证

### 已完成的验证

- ✅ ID唯一性检查
- ✅ 必填字段检查
- ✅ 选择题选项格式检查
- ✅ 复合题结构检查

### 建议的验证流程

```javascript
function validateQuestion(q) {
  const errors = [];

  // 必填字段
  ['id', 'category', 'type', 'content'].forEach(field => {
    if (!q[field]) errors.push(`缺少字段: ${field}`);
  });

  // 选择题验证
  if (['single_choice', 'multiple_choice'].includes(q.type)) {
    const opts = q.content.options;
    if (!opts || opts.length < 2) {
      errors.push('选择题至少需要2个选项');
    }
  }

  // 复合题验证
  if (q.type === 'composite') {
    const subs = q.content.subQuestions;
    if (!subs || subs.length === 0) {
      errors.push('复合题必须包含子题目');
    }
  }

  return errors;
}
```

---

## 📈 后续优化路线图

### 第一阶段（1-2周）
- [ ] 补充93道题的答案和解析
- [ ] 审核并修正42道复合题的子题类型
- [ ] 添加基础标签（人物、情节）

### 第二阶段（2-4周）
- [ ] 细化二级分类
- [ ] 人工校准难度分级
- [ ] 添加题目来源信息（回目、原文）

### 第三阶段（持续）
- [ ] 收集答题数据
- [ ] 根据数据优化难度标签
- [ ] 添加常见错误选项分析

---

## 📝 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 2.0 | 2026-03-10 | 重构分类体系，添加复合题型支持，规范化数据结构 |
| 1.0 | 2026-03-09 | 初始版本，从5个TXT文件提取 |

---

*报告生成时间: 2026-03-10 20:21:00*
